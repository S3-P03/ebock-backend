<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ebock.mapper.ReviewMapper">

    <select id="getUserAverageReview" resultType="com.ebock.dto.response.review.AverageReviewResponse">
        SELECT round(avg(r.rating), 2) AS avgRating,
               count(r.rating) AS nbrReviews
        FROM review r
        WHERE r.reviewed_cip = #{cip}
        GROUP BY r.reviewed_cip
    </select>

    <select id="getDetailledReviews" resultType="com.ebock.dto.response.review.ReviewDetailsResponse">
        SELECT u.first_name,
               u.last_name,
               u.profile_picture_guid,
               r.content,
               r.timestamp_,
               r.rating
        FROM review r
            INNER JOIN user_ u ON r.reviewer_cip = u.cip
        WHERE r.reviewed_cip = #{cip}
    </select>

    <insert id="insert">
        INSERT INTO review (reviewer_cip, reviewed_cip, timestamp_, content, rating)
        VALUES (#{reviewerCip}, #{reviewedCip}, NOW(), #{review.content}, #{review.rating})
    </insert>

    <update id="update">
        UPDATE review r
        SET content = #{review.content},
            rating = #{review.rating},
            updated_at = NOW()
        WHERE r.reviewer_cip = #{reviewerCip} AND r.reviewed_cip = #{reviewedCip};
    </update>

    <select id="getCountByUsers" resultType="int">
        SELECT count(r.rating)
        FROM review r
        WHERE r.reviewer_cip = #{reviewerCip} AND r.reviewed_cip = #{reviewedCip};
    </select>
</mapper>
