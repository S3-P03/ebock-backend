<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ebock.mapper.ItemMapper">

    <select id="getPaginatedItem" resultType="com.ebock.dto.response.item.ItemResponse">
        <bind name="offset" value="(pageNumber - 1) * pageSize" />

        SELECT i.name,
               i.price::numeric AS price,
               i.added_at,
               i.quantity,
               i.category_id,
               i.wear_id,
               u.first_name,
               u.last_name
        FROM ebock.item i
        INNER JOIN ebock.user_ u ON i.seller_cip = u.cip
        WHERE i.sold = false AND i.archived = false
        ORDER BY i.item_id
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

</mapper>
